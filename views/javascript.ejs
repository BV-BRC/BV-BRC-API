
                <script>
                        var dojoConfig = {
                                parseOnLoad: false
                        }
                </script>
                <% if (request&&request.production){ %>
                        <script src="/js/release/dojo/dojo.js"></script>
                        <script>
                        require(["cid/layer/p3api"], function(){
                <% } else { %>
                        <script src="/js/dojo/dojo.js"></script>
                        <script>
                <% } %>

                        require(["cid/widget/WorkspaceManager", "cid/widget/PatricHeader","cid/widget/BorderContainer","cid/app/apiexplorer"],function(WorkspaceManager, PatricHeader,BorderContainer,App){

			var appOpts={}

			<% if (results && results && results[0]) { %>
				appOpts.launchGridColumns=<%- JSON.stringify(Object.keys(results[0])) %>;
			<% } %>

                                app = new App(appOpts);

                        });
                <% if (request&&request.production){ %>
                        });
                <% } %>
                </script>


